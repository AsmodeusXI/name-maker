{
  "name": "name-maker",
  "version": "1.1.0",
  "description": "Randomly generates various names for Iris.",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "prebuild": "rm -rf ./dist && mkdir dist",
    "build": "tsc",
    "publish": "npm run build && rm ./dist/test.js && cp ./package.json ./dist/package.json"
  },
  "keywords": [
    "dnd5e"
  ],
  "author": "asmodeusxi",
  "license": "MIT",
  "dependencies": {
    "faucet": "0.0.1",
    "lodash": "^4.17.4",
    "tape": "^4.8.0",
    "typescript": "^2.5.2"
  },
  "devDependencies": {
    "@types/node": "^8.0.30"
  }
}
